import { UnitsearchfilterPipe } from './unitsearchfilter.pipe';

describe('UnitsearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitsearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
