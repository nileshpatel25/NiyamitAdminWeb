import { ContactussearchfilterPipe } from './contactussearchfilter.pipe';

describe('ContactussearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactussearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
