import { WholesellersearchfilterPipe } from './wholesellersearchfilter.pipe';

describe('WholesellersearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WholesellersearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
