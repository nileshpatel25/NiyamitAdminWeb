import { SmsconfigserchfilterPipe } from './smsconfigserchfilter.pipe';

describe('SmsconfigserchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SmsconfigserchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
