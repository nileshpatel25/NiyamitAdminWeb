import { PickuplocationsearchfilterPipe } from './pickuplocationsearchfilter.pipe';

describe('PickuplocationsearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PickuplocationsearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
